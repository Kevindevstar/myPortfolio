import React from "react";
import Tilt from "react-parallax-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Kevin Andres Green Marin.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
        Hi, I’m Kevin, a passionate full-stack developer based in Valledupar, Colombia, with over five years of professional experience in web and mobile development. I specialize in creating efficient, scalable, and user-friendly solutions using technologies like JavaScript, Node.js, PHP, Python, and the MERN stack.
        I’ve had the privilege of working with leading companies, including Parrolabs, AlfamediaWEB, Globant, BairesDev, and Intern Colombia, where I developed diverse projects ranging from dynamic web applications to innovative blockchain integrations.
        Beyond web development, I’m also diving into mobile app development and exploring blockchain technologies, aiming to stay at the forefront of modern development trends. My dedication to learning and adapting ensures I deliver high-quality results tailored to clients’ needs.
        Let’s build something extraordinary together!
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
